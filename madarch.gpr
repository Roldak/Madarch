with "opengl";
with "opengl-glfw";

with "gnatcoll";

library project Madarch is
   for Source_Dirs use ("madarch", "madarch/support");
   for Object_Dir use "obj";
   for Library_Dir use "lib";
   for Library_Name use "madarch";

   package Compiler is
      for Switches ("Ada") use ("-g");
   end Compiler;
end Madarch;
